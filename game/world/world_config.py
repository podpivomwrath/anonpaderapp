"""Конфиг мира: карта, перемещение, первый региональный квест.

Калибруемые константы — не хардкодить по коду.
"""

# --- Карта: сетка -50..50, Багряный Монолит в (0;0) ---
BOUNDS_MIN = -50
BOUNDS_MAX = 50

CITY_COORDS = {
    "ridge": (50, 50),        # 🏰 Обетованный Кряж
    "woods": (-50, 50),       # 🌲 Шепчущие Пущи
    "docks": (50, -50),       # ⚓ Соляные Пристани
    "scorched": (-50, -50),   # 🔥 Выжженный Предел
}

# Секунд реального времени на клетку перемещения (позже маунты ускоряют)
CELL_TRAVEL_SECONDS = 10.0

# Исследование клетки: случайная задержка [min, max] сек до появления моба
EXPLORE_SECONDS_MIN = 5.0
EXPLORE_SECONDS_MAX = 10.0

# Исход исследования (патч 13, ч.3): бой / событие с выбором. Флейвор больше
# не самостоятельный исход — остаток (1 - combat) целиком уходит на событие.
EXPLORE_COMBAT_CHANCE = 0.50

# Опыт за исход события — доля от опыта моба УРОВНЯ ЗОНЫ клетки события, не
# уровня игрока (патч 36) — см. services/experience_service.py::event_xp,
# game/world/grid.py::mob_level_at. Иначе события обесценивались на дальних
# кольцах: игрок высокого уровня получал фиксированную (по своему уровню)
# долю опыта за исход события на клетке, которая формально даёт заметно
# больше опыта за убийство обычного моба.
# Осторожный выбор (пройти мимо/оставить/не трогать) — гарантированно, но
# меньше, чем убийство моба.
EVENT_XP_SAFE = 0.5
# Рискованный выбор (коснуться/помолиться/вскрыть, ux-patch-10 блок 3) — при
# удаче примерно как за моба: риск урона сопоставим с боем.
EVENT_XP_RISKY = 1.0

# Отдых вне боя: случайное время [min, max] сек, после — полное HP
REST_SECONDS_MIN = 8.0
REST_SECONDS_MAX = 12.0

# Сложность зоны по расстоянию Чебышёва до Монолита: (dist_min, dist_max, (level_min, level_max))
# Покрывает весь диапазон 0..50 без пропусков.
ZONE_TABLE = [
    (40, 50, (1, 15)),
    (25, 39, (16, 30)),
    (12, 24, (31, 45)),
    (3, 11, (46, 60)),
    (0, 2, (60, 60)),    # центр, эндгейм — потолок уровня (патч 34, ч.0: MAX_LEVEL=60)
]

# --- Стартовое кольцо (первый квест) ---
STARTING_RING_MOB_LEVELS = (1, 3)
FIRST_QUEST_PROGRESS_LABEL = "Пепельные твари"
