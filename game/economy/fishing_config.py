"""Числа рыбалки (патч 58): вес, градации, обрыв лески, клёв, цены, садок.

Каталог (названия/эмодзи/лор/координаты озёр) — content/fishing/fish.json и
content/fishing/lakes.json. Здесь ТОЛЬКО калибруемые числа, как у эликсиров
(game/economy/elixir_config.py): контент правится редактором текста, баланс —
симулятором tools/sim_fishing.py, и они не мешают друг другу.

Главное отличие рыбалки от остальных источников золота: у неё НЕТ расходника
и НЕТ суточного лимита (сознательное решение — рыбаки должны быть отдельной
кастой). Значит доход целиком держится на двух числах: длительности цикла и
цене за килограмм. Ошибка в них не гасится ничем, поэтому калибровать их
можно только симулятором, не на глаз.

Измерено tools/sim_fishing.py (доход в % от фарма мобов на том же кольце,
продажа Иргалу, с учётом дороги до города при полном садке):

    тир озера    ур.1   ур.25   ур.50  ур.100  ур.200
        I         56%     73%     83%     93%     96%
        II        43%     64%     76%     89%     95%
        III       28%     50%     64%     79%     87%
        IV        17%     40%     56%     69%     78%
        V         17%     33%     49%     67%     76%

Три свойства этой таблицы обязаны сохраниться при любой правке чисел:
  1. рыбалка НИГДЕ не обгоняет бой (потолок 96%, и то на самом бедном кольце);
  2. в абсолюте лучший рыбак (2582 зол/час) ниже лучшего бойца (3384);
  3. глубокое озеро наказывает новичка (17%) и окупается только мастерством —
     формального запрета там рыбачить нет и не должно быть.

Наценка ивента «Рыбак у воды» (110-400%) в таблицу НЕ входит: это верхняя
надбавка за то, что игрок потащил улов в опасное кольцо, а не базовый доход.
"""

# --- Кольца сложности озера -------------------------------------------------
# Тир озера 1..5 соответствует кольцу карты (world_config.ZONE_TABLE):
# 1 — dist 40-50 (1-15 ур.), 2 — 25-39, 3 — 12-24, 4 — 3-11, 5 — 0-2.
# Тир 6 (Безымянное) не принадлежит озеру: это редкость, доступная везде.

#: PvP на клетке озера запрещён, пока тир озера не выше этого (кольца I и II).
#: Первые мирные клетки в игре вне городов — см. services/fishing_service.py
#: ::is_safe_lake и проверки в bot/handlers/pvp.py.
PVP_SAFE_MAX_LAKE_TIER = 2


# --- Виды рыбы --------------------------------------------------------------
#: fish_id -> (min_grams, max_grams, gold_per_kg)
#: Цена ДРОБНАЯ и намеренно нигде не показывается игроку: он видит только
#: стоимость стака целиком. Целочисленная цена ломала соотношения внутри
#: тира при округлении, а поштучный расчёт вообще упирался в минимум в
#: 1 золотой почти у половины мелкой рыбы (измерено tools/sim_fishing.py).
#: Значения подобраны tools/sim_fishing.py --calibrate, не на глаз.
#: Вес ВСЕГДА в граммах целым числом: float в БД рассинхронизировал бы рекорды
#: при пересчётах (та же грабля, что с округлением в мини-аппе, патч 57).
FISH_STATS: dict[str, tuple[int, int, float]] = {
    # тир 1
    "ashen_roach":      (200, 1200, 8.83),
    "blind_gudgeon":    (100, 700, 7.39),
    "pale_perch":       (300, 2200, 13.27),
    "reed_spinefish":   (100, 500, 5.9),
    # тир 2
    "brackish_bream":   (800, 4500, 6.91),
    "rusty_tench":      (500, 3500, 8.45),
    "whisper_chub":     (600, 3000, 9.99),
    "sewer_lamprey":    (300, 1800, 11.51),
    # тир 3
    "bone_pike":        (2000, 11000, 5.61),
    "black_catfish":    (3000, 18000, 4.97),
    "marked_carp":      (1500, 9000, 6.84),
    "taint_eel":        (1000, 6000, 8.1),
    # тир 4
    "ashen_sturgeon":   (5000, 30000, 6.13),
    "drowned_flounder": (2000, 14000, 7.48),
    "crimson_burbot":   (3000, 20000, 7.13),
    # тир 5
    "deep_tear":        (500, 4000, 18.73),
    "monolith_catfish": (10000, 60000, 8.42),
    # тир 6 — вне озёрных пулов, см. NAMELESS_CHANCE
    "the_nameless":     (500, 25000, 11.72),
}

#: Пул видов озера по тиру: fish_id -> вес в жеребьёвке. Суммы нормализуются,
#: так что это просто пропорции. Небольшая протечка вида следующего тира —
#: чтобы на любом озере оставался шанс на что-то лучше обычного.
LAKE_POOLS: dict[int, dict[str, float]] = {
    1: {"ashen_roach": 34, "blind_gudgeon": 28, "pale_perch": 22,
        "reed_spinefish": 13, "brackish_bream": 3},
    2: {"brackish_bream": 33, "rusty_tench": 27, "whisper_chub": 21,
        "sewer_lamprey": 16, "bone_pike": 3},
    3: {"bone_pike": 31, "black_catfish": 27, "marked_carp": 22,
        "taint_eel": 17, "ashen_sturgeon": 3},
    4: {"ashen_sturgeon": 38, "drowned_flounder": 30, "crimson_burbot": 29,
        "deep_tear": 3},
    5: {"deep_tear": 42, "monolith_catfish": 38, "crimson_burbot": 20},
}

#: Патч 62: уровень рыбалки, с которого открывается вид рыбы своего тира.
#: Раньше вид зависел ТОЛЬКО от озера — рыбак 9-го уровня ловил у Монолита
#: Слезу глубины и Монолитова сома, то есть эндгеймовую рыбу. Обрыв лески в
#: 92% этого не останавливал: заброс стоит полминуты, и один успех из
#: двенадцати — это шесть минут за трофей. Вероятностный гейт не работает,
#: когда попытка почти ничего не стоит.
#:
#: Шкала намеренно совпадает с горным делом (mining_config.MINE_REQUIRED_LEVEL):
#: два разных порога на одно и то же «дорос или нет» игрок бы не запомнил.
LAKE_REQUIRED_LEVEL: dict[int, int] = {1: 1, 2: 10, 3: 25, 4: 50, 5: 80}

#: Безымянное — единственный вид, доступный на ЛЮБОМ озере. Проверяется до
#: розыгрыша пула: выпало — пул не разыгрывается вовсе.
NAMELESS_ID = "the_nameless"
NAMELESS_CHANCE = 0.003


# --- Вес и градация ---------------------------------------------------------
# Вес внутри диапазона вида: weight = min + (max - min) * U**k.
# k > 1 смещает распределение к мелочи. Уровень рыбалки уменьшает k, но
# АСИМПТОТИЧЕСКИ: k никогда не доходит до 1.0, иначе на больших уровнях
# трофеи стали бы рутиной, а вся ценность градаций обнулилась.
#     k(L) = WEIGHT_K_FLOOR + WEIGHT_K_SPAN * WEIGHT_K_DECAY ** (L - 1)
#
# Патч 58, калибровка: span был 1.10 при decay 0.97 — симулятор показал, что
# доход от этого рос в 2.7 раза с 1-го по 50-й уровень, и попасть в целевой
# коридор хоть на каком-то уровне стало невозможно (на одном конце нищета, на
# другом — обгон фарма мобов). Уменьшенный span делает вклад уровня мягче,
# увеличенный decay растягивает прогресс: уровень бесконечен, и выбирать весь
# запас роста за первые 50 уровней нельзя.
WEIGHT_K_FLOOR = 1.50
WEIGHT_K_SPAN = 0.70
WEIGHT_K_DECAY = 0.985

#: Градация — производная от доли веса f = (weight - min) / (max - min),
#: не отдельный бросок. (нижняя граница f, id, название, множитель цены).
#: Порядок — от мелкой к крупной, поиск идёт с конца.
#:
#: Пороги подняты после первого прогона симулятора: при (0.60/0.85/0.97) даже
#: на 1-м уровне трофей выпадал в 6-7% случаев. Распределение U**k давит вес к
#: низу, но хвост остаётся жирным, и «трофей» переставал быть событием. Сейчас
#: трофей ~4% на низких уровнях и ~5% на высоких, легендарная — около 0.5%.
GRADES: list[tuple[float, str, str, float]] = [
    (0.00, "small", "мелочь", 0.6),
    (0.25, "common", "обычная", 1.0),
    (0.70, "big", "крупная", 1.5),
    (0.92, "trophy", "трофейная", 2.5),
    (0.99, "legendary", "легендарная", 5.0),
]

GRADE_EMOJI = {
    "small": "·", "common": "", "big": "▲", "trophy": "🏆", "legendary": "👑",
}


# Рыбалка НЕ блокирует остальные действия — в отличие от горного дела. Это
# решение, а не недосмотр: цикл ловли длится секунды, а добычи — минуты, и
# запрещать на время одного заброса ходить и исследовать было бы мелочной
# опекой. Обратная сторона принята сознательно: заброшенная снасть переживает
# уход с клетки и протухает сама (services/fishing_service.py::cast_is_stale).
#
# Если когда-нибудь рыбалку решат уравнять с добычей, менять надо ТРИ места:
# bot/activity.py::blocked_reason и собственные проверки move/explore/rest в
# bot/handlers/world.py — общий гейт эти три обработчика не вызывают.


# --- Клёв: ожидание с разбросом ---------------------------------------------
# Равномерное ожидание быстро надоедает. Три режима с разной длительностью,
# и ДЛИННОЕ ожидание смещает вес вверх — пауза перестаёт быть пустой.
# (вес режима, секунд min, секунд max, бонус к доле веса f).
BITE_MODES: list[tuple[float, float, float, float]] = [
    (0.70, 6.0, 15.0, 0.00),
    (0.22, 15.0, 40.0, 0.07),
    (0.08, 40.0, 90.0, 0.18),
]

# Пустых забросов НЕТ: поклёвка случается всегда, вопрос только когда.
# Решение продуктовое — «ничего не произошло» это тот же пустой исход, что
# запрещён в событиях исследования правилом патча 10. Риск даёт обрыв лески,
# а не отсутствие клёва.

#: Сколько секунд у игрока есть на подсечку после поклёвки. Подсечка —
#: единственная защита от AFK-фарма (расходника у рыбалки нет), поэтому она
#: обязательна, а не опциональна. С запасом на задержки VK.
STRIKE_WINDOW_SECONDS = 12.0

#: Пауза между забросами.
CAST_COOLDOWN_SECONDS = 8.0

#: Ориентировочная длина полного цикла для прикидок в документации. Симулятор
#: считает её честно, на это значение не опирается.
NOMINAL_CYCLE_SECONDS = 40.0


# --- Обрыв лески: мягкий гейт вместо запрета --------------------------------
# Формального запрета рыбачить на дальних озёрах НЕТ. Новичок у Монолита
# просто рвёт леску почти всегда и видит, как что-то уходит. Это же —
# единственное, на что уровень рыбалки влияет кроме веса.
#     обрыв = clamp(BASE[тир] - reduction(L) + f * BY_WEIGHT, MIN, MAX)
#     reduction(L) = REDUCTION_MAX * (1 - REDUCTION_DECAY ** L)
#
# Патч 58, калибровка: вычет был ЛИНЕЙНЫЙ (L * 0.006), и к 125-му уровню он
# съедал даже базу 0.75 самого глубокого озера — мастер-рыбак у Монолита не
# встречал вообще никакого сопротивления и зарабатывал впятеро больше лучшего
# бойца в игре. Затухающий вычет упирается в REDUCTION_MAX: сложное озеро
# остаётся сложным на ЛЮБОМ уровне, просто перестаёт быть безнадёжным.
# REDUCTION_MAX подобран вторым прогоном: при 0.45 рыбак 100-го уровня на
# озере у Монолита давал 143% от лучшего фарма мобов, то есть выходил за
# согласованный коридор. При 0.38 обрыв на пятом тире не опускается ниже
# ~55% никогда, и потолок дохода остаётся в пределах ~115%.
LINE_BREAK_BASE: dict[int, float] = {1: 0.05, 2: 0.18, 3: 0.38, 4: 0.58, 5: 0.75}
LINE_BREAK_REDUCTION_MAX = 0.38
LINE_BREAK_REDUCTION_DECAY = 0.975
LINE_BREAK_BY_WEIGHT = 0.35
LINE_BREAK_MIN = 0.02
LINE_BREAK_MAX = 0.92


# --- Уровень рыбалки: без потолка -------------------------------------------
#: Опыта до следующего уровня: round(XP_BASE * L ** XP_EXP). Потолка нет —
#: каждый следующий уровень дороже предыдущего, расти можно бесконечно.
FISHING_XP_BASE = 50
FISHING_XP_EXP = 1.85

#: Опыт за пойманную рыбу: TIER_XP[тир] * (1 + XP_WEIGHT_BONUS * f).
FISHING_TIER_XP: dict[int, int] = {1: 4, 2: 12, 3: 35, 4: 90, 5: 250, 6: 300}
FISHING_XP_WEIGHT_BONUS = 2.0

#: Опыт за сорвавшуюся рыбу — доля от того, что дала бы пойманная. Обрыв не
#: должен быть полностью пустым: иначе низкий уровень на сложном озере не
#: только ничего не ловит, но и не растёт, и выбраться из ямы нечем.
FISHING_XP_ON_BREAK = 0.25


# --- Садок ------------------------------------------------------------------
# Меряется в КИЛОГРАММАХ, не в штуках: рыба стакается по (вид + градация)
# суммарным весом, отдельных хвостов в БД нет вовсе.
#     кг = BAG_BASE_KG + BAG_SPAN_KG * (1 - BAG_DECAY ** L)
BAG_BASE_KG = 40.0
BAG_SPAN_KG = 160.0
BAG_DECAY = 0.98


# --- Находки вместо рыбы ----------------------------------------------------
#: Шанс, что вместо рыбы со дна пришло что-то другое. Разыгрывается ПОСЛЕ
#: успешной подсечки — то есть находка всегда поймана, её не рвёт леской.
JUNK_CHANCE = 0.06

#: Хлам: id -> (эмодзи, название, цена в золоте). Продаётся сразу, в садок
#: не кладётся (у него нет ни веса, ни градации).
JUNK_ITEMS: dict[str, tuple[str, str, int]] = {
    "soggy_boot": ("🥾", "Разбухший сапог", 1),
    "driftwood": ("🪵", "Топляк", 1),
    "rusty_hook": ("⚙️", "Ржавый крюк", 2),
}

#: Внутри находки: сундук даёт бросок трофеев, редко — снаряжение/ключ.
#: Остаток до 1.0 — обычный хлам из JUNK_ITEMS.
JUNK_CHEST_CHANCE = 0.30
JUNK_ITEM_CHANCE = 0.03
JUNK_RAID_KEY_CHANCE = 0.01


# --- Продажа ----------------------------------------------------------------
#: Иргал берёт рыбу, но он скупщик пепла, а не рыбник — отсюда скидка.
#: Это гарантированный ПОЛ цены, доступный в любом городе всегда.
APPRAISER_FISH_MULTIPLIER = 0.60

#: Ивент «Рыбак у воды» (content/events/exploration.json) — потолок цены.
#: Наценка зависит от кольца КЛЕТКИ, где выпал ивент, а не от того, где рыба
#: поймана: отсюда весь цикл «налови в безопасном кольце, дотащи в опасное».
#: тир кольца -> (минимальный множитель, максимальный).
BUYER_EVENT_MARKUP: dict[int, tuple[float, float]] = {
    1: (1.10, 1.40),
    2: (1.40, 1.80),
    3: (1.80, 2.30),
    4: (2.30, 3.00),
    5: (3.00, 4.00),
}

# Порога на размер садка НЕТ: рыбак выпадает при любом улове, в том числе
# пустом. С пустым садком он просто говорит, что предложить нечего, и сцена
# заканчивается — пустым исходом события это не является, игрок получает
# внятный ответ, а не «ничего не произошло».
