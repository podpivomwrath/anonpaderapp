"""Ключ Монолита (патч 45, ч.4) — задел под будущий рейд. Числа — в конфиг,
не хардкодить в сервисе."""

RAID_KEY_DROP_CHANCE = 0.005  # 0.5%, независимый бросок с ЛЮБОГО источника лута
RAID_KEY_CAP = 2              # максимум в инвентаре — сверх капа не выпадает вовсе
# Патч 50: Метка Хранителя — лимит НАКОПЛЕНИЯ (не владения). При истечении
# премиума уже полученные ключи сверх RAID_KEY_CAP НЕ отбираются — см.
# services/raid_key_service.py::maybe_grant (просто не выпадают новые, пока
# count не опустится ниже действующего на тот момент лимита).
RAID_KEY_CAP_PREMIUM = 4
